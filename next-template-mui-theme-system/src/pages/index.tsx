import HomePage from '@/pages/HomePage';
import { Box } from '@mui/material';
import { NextPage } from 'next';

const Home: NextPage = () => {
	return (
		<Box>
			<HomePage />
		</Box>
	);
};

export default Home;
